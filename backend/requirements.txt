# API framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL driver

# Auth (Clerk JWT verification)
python-jose[cryptography]>=3.3.0

# Validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# File uploads (multipart form support) + static file serving
python-multipart>=0.0.9
aiofiles>=23.0.0

# AI providers — OpenAI-compatible (TritonAI) + Anthropic direct fallback
openai>=1.0.0
anthropic>=0.25.0

# Image processing — resize/validate before sending to AI
Pillow>=10.0.0

# PDF text extraction — extracts embedded text from PDFs without needing Ghostscript
# Scanned PDFs (no embedded text) fall back to the vision model
pypdf>=4.0.0

# Background jobs — soft-delete cleanup
apscheduler>=3.10.0

# Rate limiting
slowapi>=0.1.9

# Environment variables
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0      # async HTTP client for FastAPI test client
click>=8.1.0       # CLI flags for test harness
